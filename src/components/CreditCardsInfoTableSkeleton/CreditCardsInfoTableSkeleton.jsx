import {
    DataTableSkeleton,
} from "carbon-components-react";

const CreditCardsInfoTableSkeleton = () => {
    return (
        <DataTableSkeleton columnCount={8} rowCount={7} />
    );
}

export default CreditCardsInfoTableSkeleton;