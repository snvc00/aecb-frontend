import {
    DataTableSkeleton,
} from "carbon-components-react";

const ClientInfoTableSkeleton = () => {
    return (
        <DataTableSkeleton columnCount={11} rowCount={7} />
    );
}

export default ClientInfoTableSkeleton;